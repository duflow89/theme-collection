@../../AGENTS.md

# Antigravity workspace rule

Treat the imported repository contract as always-on guidance. Use the matching resource under `.agents/skills`, `.agents/workflows`, or `.agents/agents` when the task fits it.
